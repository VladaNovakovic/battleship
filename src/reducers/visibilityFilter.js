const visibilityFilter = (state = 'STATE') => {
    switch (state) {
        case 'SOMETHING':
            return state;
        default:
            return state;
    }
};

export default visibilityFilter;
